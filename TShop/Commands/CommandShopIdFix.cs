using Rocket.API;
using SDG.Unturned;
using System.Collections.Generic;
using System.Linq;

namespace Tavstal.TShop
{
    /*public class CommandShopIdFix : IRocketCommand
    {
        public AllowedCaller AllowedCaller => AllowedCaller.Both;
        public string Name => "shopidfix";
        public string Help => "This command changes vehicle ids to item ids if their asset does not exist and vica versa.";
        public string Syntax => "";
        public List<string> Aliases => new List<string>();
        public List<string> Permissions => new List<string> { "tshop.shopidfix" };

        public void Execute(IRocketPlayer caller, string[] args)
        {
           
        }
    }*/
}
